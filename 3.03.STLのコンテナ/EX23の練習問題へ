mapなどの辞書型とqueueなどのデータ構造についての説明

mapは.atでアクセスするのと[]でアクセスするのとでは、
要素にないものにアクセスした際にエラーになるか新規に追加されるかで差がある

dequeueは双方向から取り出しできるキュー
stack先に入れたモノを後に出すデータ構造

unordered_set,mapはハッシュソートによるコンテナ
mapは通常キーがソートされているが、unorderdはハッシュ値が割り当てられるため、順序がばらばら
ただし、検索がないため高速に参照可能
